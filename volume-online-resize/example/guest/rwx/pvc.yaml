apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: v1
  namespace: default
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: harvester-rwx
  volumeMode: Filesystem
